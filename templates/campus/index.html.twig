{% extends 'base.html.twig' %}

{% block title %}GESTION DES CAMPUS{% endblock %}

{% block header %}

{% endblock %}

{% block nav %}
    {% if is_granted ('IS_AUTHENTICATED_REMEMBERED') %}

    <li><a href="{{ path('home') }}">ACCUEIL</a></li>
    <li><a href="#">MON PROFIL</a></li>
    <li ><a href="{{ path('logout') }}">SE DECONNECTER</a></li>
    {% endif %}
{% endblock %}

{% block main %}
    {% for u in allcampus %}
        <div>
            {{ loop.index }}<input type="text"  value="{{ u.nomCampus }}"><a href=" {{ path('modifier_campus',{'id':u.id, 'nom':u.nomCampus}) }}">modifier </a><a href=" {{ path('delete_campus',{'id':u.id}) }}"> supprimer</a>


        </div>
    {% endfor %}
    {{ form(CampusForm) }}<button type="submit">Send!</button>

{% endblock %}